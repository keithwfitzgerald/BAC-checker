# BAC-checker
BAC checker

BAC= input("enter BAC: ")
print("Are you over or under .05?")
input("yes or no")
over_limit = True
below_limit = True

if over_limit:
    print("cannot drive")
    
if not(over_limit):
    print("can drive")
